# Deploy (Compose Skeleton)

Local/dev orchestration reference. For production use managed infra (ECS/Fargate, Fly.io, Render).

- Build & push images
- Run migrations
- Start API, Worker, Redis, DB, Object Storage
- Configure webhooks using PUBLIC_BASE_URL
